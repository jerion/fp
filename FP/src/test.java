import card.Atk_Card;
import card.Card;
import card.Effect_Card;

public class test {
	public static void main (String[] args)
	{
		Card cardlist[] = new Card[2];
		cardlist[0].set_AttackCard(Name, Element, Level, Mana, Attack);
		cardlist[1].set_EffectCard(Name, Element, Level, Mana, AddMana, AddCard);
	}
}
